<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Misty Dungeon - Grozs</title>
  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Cinzel', serif;
      background: url('/images/dungeon-bg.jpg') no-repeat center center fixed;
      background-size: cover;
      color: #f8f8f2;
      padding: 2em;
    }
    .cart-item {
      background: rgba(0,0,0,0.75);
      border: 2px solid #b8860b;
      border-radius: 10px;
      padding: 1em;
      margin-bottom: 1em;
    }
    .cart-item h2 {
      margin: 0 0 0.5em 0;
      color: #ffd700;
    }
    .cart-item p {
      margin: 0.3em 0;
    }
    .checkout-btn {
      background-color: #ffd700;
      color: #000;
      font-family: 'Cinzel', serif;
      border: none;
      padding: 0.8em 1.5em;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1.2rem;
    }
    .checkout-btn:hover {
      background-color: #ffcc00;
    }
  </style>
</head>
<body>
  <h1>Misty Dungeon - Tavs grozs</h1>

  <% if(Object.keys(cart).length === 0) { %>
    <p>Grozs ir tukšs.</p>
  <% } else { %>
    <% Object.values(cart).forEach(item => { %>
      <div class="cart-item">
        <h2><%= item.name %></h2>
        <p>Cena: <%= item.price %>€</p>
        <p>Daudzums: <%= item.quantity %></p>
        <p>Kopā: <%= (item.quantity * parseFloat(item.price)).toFixed(2) %>€</p>
      </div>
    <% }) %>

    <form action="/pay/cart" method="post">
      <button type="submit" class="checkout-btn">Pirkt visu grozu</button>
    </form>
  <% } %>

  <a href="/" style="color:#ffd700;">Atpakaļ uz veikalu</a>
</body>
</html>
