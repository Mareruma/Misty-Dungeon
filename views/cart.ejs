<!DOCTYPE html>
<html lang="lv">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Grozs - Misty Dungeon</title>
  <style>
    body { margin:0; padding:2em; font-family:'Cinzel',serif; background:#1c1c1c; color:#f8f8f2; }
    h1 { text-align:center; color:#f0e68c; }
    .cart-item { background:#2b2b2b; padding:1em; margin:1em auto; border-radius:8px; max-width:500px; border:1px solid #b8860b; }
    .btn { background:#ffd700; border:none; padding:0.4em 0.8em; border-radius:5px; cursor:pointer; }
    .btn:hover { background:#ffcc00; }
    .total { text-align:center; margin-top:1em; }
    .checkout { display:block; margin:1em auto; text-align:center; }
  </style>
</head>
<body>
  <h1>Tavs grozs</h1>

  <% if (cart.length === 0) { %>
    <p style="text-align:center;">Grozs ir tukšs</p>
  <% } else { %>
    <% cart.forEach(item => { %>
      <div class="cart-item">
        <h2><%= item.name %></h2>
        <p>Cena: <%= item.price %>€</p>
        <form action="/cart/remove" method="post">
          <input type="hidden" name="id" value="<%= item._id %>">
          <button class="btn" type="submit">Noņemt</button>
        </form>
      </div>
    <% }) %>

    <div class="total">
      <h2>Kopā: <%= total %>€</h2>
    </div>

    <div class="checkout">
      <form action="/pay" method="post">
        <button class="btn" type="submit">Apmaksāt ar PayPal</button>
      </form>
    </div>
  <% } %>

  <div style="text-align:center; margin-top:20px;">
    <a href="/" style="color:#ffd700;">Turpināt iepirkšanos</a>
  </div>
</body>
</html>
